{% extends "base.html.twig" %}

{% block title %}Publier un nouveau projet{% endblock %}

{% block body %}

    <div class="container mt-4">
        <h1>Publier un projet</h1>

        {{ form_start(form) }}
        {{ form_rest(form) }}
        <div class="col-4">
            <button class="btn btn-primary">Créer</button>
            <a href="{{ path('back_projet_user_projet_new') }}" class="btn btn-secondary">Retour</a>
        </div>
        {{ form_end(form) }}
    </div>

{% endblock %}